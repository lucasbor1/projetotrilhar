<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Estado quando o switch está ativado (pago) -->
    <item android:color="@color/verde" android:state_checked="true" />
    <!-- Estado quando o switch está desativado (não pago) -->
    <item android:color="@color/vermelho" />
</selector>
